<div class="section-page">
	<div class="section-header">
		<span class="section-label">THOUGHTS</span>
		<span class="section-count">00</span>
	</div>
	<div class="empty-row">
		<span class="empty-text">nothing yet</span>
	</div>
</div>

<style>
	.section-page {
		width: 100%;
	}
	.section-header {
		display: flex;
		justify-content: space-between;
		align-items: baseline;
		padding-bottom: 0.75rem;
		margin-bottom: 0.25rem;
		border-bottom: 1px solid color-mix(in srgb, var(--color-sub) 30%, transparent);
	}
	.section-label {
		font-size: 0.7rem;
		letter-spacing: 0.3em;
		color: var(--color-text-bright);
		font-weight: 600;
	}
	.section-count {
		font-size: 0.7rem;
		color: var(--color-text-bright);
	}
	.empty-row {
		padding: 2.5rem 0;
	}
	.empty-text {
		font-size: 0.85rem;
		color: var(--color-text-bright);
		font-style: italic;
	}
</style>
